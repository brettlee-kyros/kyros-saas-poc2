# ============================================================================
# Kyros SaaS PoC - Environment Variables Template
# ============================================================================
# Copy this file to .env.local and update values as needed
# cp .env.example .env.local

# JWT Configuration
# ⚠️ CRITICAL: Change this secret in production!
# For PoC, this matches the hardcoded value in shared-config/config.py
JWT_SECRET_KEY=kyros-poc-secret-key-CHANGE-IN-PRODUCTION-12345678

# Database Configuration
# Path to SQLite database file (relative to project root)
DATABASE_PATH=/app/data/tenant_metadata.db

# API Configuration
# Base URL for FastAPI backend
# Docker: http://api:8000 (service name)
# Local: http://localhost:8000
API_BASE_URL=http://api:8000

# Next.js Configuration
NODE_ENV=production

# Python Configuration
PYTHONUNBUFFERED=1

# Server Ports (for local development)
SHELL_UI_PORT=3000
API_PORT=8000
DASH_CLV_PORT=8050
DASH_RISK_PORT=8051
